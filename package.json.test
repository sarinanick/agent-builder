{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "playwright test",
    "qa": "npm run typecheck && npm run lint && npm run test:run"
  }
}
